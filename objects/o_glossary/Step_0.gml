c_input();
if string_copy(entries[pos].info, 0, 2) != "Lo" {
	c
}
if clamp(pos+rightp-leftp, 0, array_length(entries)-1) != pos count = 0;
pos = clamp(pos+rightp-leftp, 0, array_length(entries)-1);
if back {room_goto(TITLE) c_flash(global.surfaces.HUD, 120, c_white);}
/// @description Insert description here
// You can write your code in this editor
//this thing should only hold onto info and execute the elements and cursor steps, dont put code in here.
UIElements = [];
depth = -201

arrlen = 0;

function destroy(uiElement){
	for(var i = 0; i < array_length(UIElements);i++){
		if(UIElement == UIElements[i]){
			array_delete(uiElements,i,1);
			return;
		}
	}
}